<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notification & Network Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <!-- Default FCM Channel for System-intercepted Background Notifications -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="rnnos_default_channel" />

        <!-- Firebase Messaging Service -->
        <service
            android:name="com.rnnos.core.services.RNNOSMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Broadcast Receiver for Inline Reply & Notification Action Buttons -->
        <receiver
            android:name="com.rnnos.core.renderers.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.rnnos.ACTION_INLINE_REPLY" />
                <action android:name="com.rnnos.ACTION_BUTTON_CLICK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
